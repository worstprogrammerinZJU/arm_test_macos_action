# .github/workflows/asm-test.yml
name: Test macOS ARM Assembly

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test-assembly:
    runs-on: macos-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'
        cache: 'pip'
    
    - name: Install system dependencies
      run: |
        # 安装 QEMU
        brew install qemu
        
        # 验证工具安装
        which clang
        which qemu-aarch64
        python3 --version
    
    - name: Install Python dependencies
      run: |
        # 安装 requirements.txt 中的所有依赖
        pip install -r requirements.txt
        
        # 验证安装
        pip list
    
    - name: Display environment info
      run: |
        echo "=== 环境信息 ==="
        uname -a
        sw_vers
        echo ""
        echo "=== 找到的汇编文件 ==="
        find . -name "*.s" -o -name "*.S" | head -10
        echo ""
        echo "=== 当前目录 ==="
        ls -la
    
    - name: Run the Python test script
      run: |
        # 给脚本执行权限
        chmod +x batch_test_new.py
        
        # 运行测试脚本
        python3 batch_test_new.py . --verbose
    
    - name: Upload test results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: test-results
        path: test_output_macos/
        retention-days: 7
    
    - name: Upload individual test artifacts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: compiled-files
        path: |
          **/*.o
          **/test_*
        retention-days: 3
      continue-on-error: true
    
    - name: Generate summary
      if: always()
      run: |
        echo "=== 测试总结 ==="
        echo "工作流状态: ${{ job.status }}"
        
        if [ -d "test_output_macos" ]; then
          echo "✅ 测试输出目录存在"
          echo "输出目录内容:"
          ls -la test_output_macos/
          
          if [ -f "test_output_macos/test_report.txt" ]; then
            echo ""
            echo "📊 测试报告摘要:"
            cat test_output_macos/test_report.txt | head -20
          fi
        else
          echo "❌ 测试输出目录不存在"
        fi
